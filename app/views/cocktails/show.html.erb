

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cocktail.photo.attached? ? (cl_image_path cocktail.photo.key, height: 300, width: 400, crop: :fill) : 'https://loremflickr.com/cache/resized/65535_50313310487_1c15fca663_320_240_nofilter.jpg'%>);">
  <div class="container">
    <h1><%= @cocktail.name %> <strong>will fuck you up</strong> <strong></strong>!</h1>
    <p>Change your life and learn to code at one of our campuses around the world.</p>
    <a class="btn btn-flat" href="#">Apply now</a>
  </div>
</div>


<h1><%= @cocktail.name %></h1>
 <% @cocktail.doses.each do |dose|%>
 <div class="d-flex">
 <%= dose.ingredient.name %>
 <p>  - <%= dose.description %></p>
 <%= link_to 'Delete', dose_path(dose), method: :delete, data: {confirm: 'are u sure?'}, class: 'text-danger' %>
</div>
<% end %>


<%= link_to 'add ingredient', new_cocktail_dose_path(@cocktail)  %>
